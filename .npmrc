# Ensure typescript is hoisted to root node_modules.
# Required by packages/vscode-lit-plugin/copy-to-built.js which copies
# typescript files from ../../node_modules/typescript into the extension bundle.
public-hoist-pattern[]=typescript
